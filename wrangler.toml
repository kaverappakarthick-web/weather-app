name = "weather-app"
compatibility_date = "2025-01-01"
pages_build_output_dir = "dist"

# KV namespace for AI response caching — optional, app works without it
# To enable: run `npx wrangler kv namespace create AI_CACHE`
# then uncomment and fill in the IDs below
# [[kv_namespaces]]
# binding    = "AI_CACHE"
# id         = "YOUR_KV_NAMESPACE_ID"
# preview_id = "YOUR_KV_NAMESPACE_PREVIEW_ID"
